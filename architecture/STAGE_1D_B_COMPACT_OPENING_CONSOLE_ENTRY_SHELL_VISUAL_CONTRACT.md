# Stage 1D-B — Compact Opening Console-Entry Shell Visual Contract

**Status:** CANDIDATE V1 FOUNDER REJECTED — RESPONSIVE VISUAL DRIFT; ITERATION 2 FOUNDER VISUALLY APPROVED AND BYTE-LOCKED AS `VPB-HOME-016`; PRODUCTION REMAINS LOCKED
**Founder authorization:** `FOUNDER AUTHORIZES STAGE 1D-B COMPACT ITERATION 2 DOCUMENTATION AND VISUAL-LOCK CHECKPOINT ONLY`
**Founder approval:** `FOUNDER APPROVES STAGE 1D-B COMPACT ITERATION 2`
**Viewport:** exactly `1100 × 800`
**Active lane:** Lane 0 — governance/visual-contract control
**Active production Build Contract:** NONE

## Protected authority relationship

- `VPB-HOME-008A` controls the compact environment.
- `VPB-HOME-009A` controls compact REV placement, floor contact and contact shadow.
- `VPB-HOME-011` controls compact live-brand placement.
- `VPB-HOME-014` controls the raster pathway-icon family.
- `VPB-HOME-015` controls the approved semantic blank-state structure and desktop visual character.
- `VPB-HOME-006` remains the underlying blank-state composition reference only. It is flattened evidence and cannot become a runtime or candidate interface layer.

All protected inputs remain separate layers. This contract grants no authority to alter, crop, recolour, sharpen, tune, regenerate or replace them.

## Compact geometry boundary

- Viewport: `1100 × 800`.
- Maximum available console envelope: `X=40–728`, `Y=390–700`; `688 × 310`. This envelope is not required filled geometry.
- Iteration 2 target console: approximately `X=40–728`, `Y=390–620`; `688 × 230`.
- Compact panel hierarchy: approximately `62:38`; intent panel approximately `426 × 230`, disabled entry panel approximately `262 × 230`.
- Desktop `54:46` pixels do not control compact geometry.
- Console-to-REV clearance: `81px`.
- No outer-width or REV-clearance amendment is authorized.
- Empty transition floor: `Y=620–700`.
- Empty visible future-station corridor: `Y=700–800`.
- Future-station overlap: `0px`.
- Protected REV floor contact: `X=965`, `Y=610`.
- Protected receiving-hand target: `X=826`, `Y=283`.

Both console panels must remain visible and legible inside the maximum envelope. The intent panel remains visually primary and the disabled entry panel remains secondary. The console must remain wide and shallow. Stacking or any other structural reflow requires separate founder authorization.

## Candidate V1 rejection

Compact Candidate V1 is **FOUNDER REJECTED — RESPONSIVE VISUAL DRIFT**. Its clean-candidate SHA-256 remains `EF803E49C46F1887AFF6BCC0213950C61881B74CB349ADDCAFD7CA3BB967D42D`.

Candidate V1 remains unchanged temporary rejected evidence outside the repository. It is not visual authority, fallback, inspiration or an Iteration 2 starting image. Its defects were separation of hero from brand, full-height corridor occupation, narrow vertical pathway cards, undersized icons and wording, excessive whole-panel suppression, weakened receiving-hand relationship, isolated console placement and failure to preserve the approved Home hierarchy.

## Corrected compact hierarchy

- The brand remains at its protected `VPB-HOME-011` placement.
- The hero remains visually connected beneath the brand on the left, targeting approximately `X=40–690`, `Y=148–280`.
- The trust row targets approximately `X=40–520`, `Y=310–362`.
- The console remains wide and shallow within its maximum envelope.
- Three pathway cards remain in one row, targeting approximately `129 × 150–158` each.
- All three icon-contain boxes are identical at approximately `92 × 62`.
- Per-icon scaling, alteration, padding adjustment, brightness change or individual tuning is prohibited.
- The bottom instruction receives its own live-text lane with at least `8–10px` border clearance.

## Disabled-panel rule

Whole-panel opacity reduction that obscures content is prohibited. The heading and supporting description remain readable at approximately `55–65%` visual strength. The textarea, `ADD PHOTO OR SKETCH` and `ASK REV` are subdued individually and remain recognizable genuine disabled controls.

## Shared-structure invariant

- Desktop and compact must use one semantic DOM structure.
- Only responsive CSS sizing, placement, spacing and typography may differ.
- No alternate compact wording, controls, assets or application logic is permitted.
- Environment, REV, contact shadow, brand, hero, trust row, console, controls and decorative icons remain separate render layers.
- Only the environment, transparent REV and three decorative pathway icons may be image layers.
- All interface wording, cards, borders and controls remain live semantic HTML/CSS.

## Required compact blank-state content

- Live canonical hero introduction and trust wording.
- Three unselected semantic pathway controls; nothing may be preselected.
- Byte-identical `VPB-HOME-014` raster icons, proportionally contained, with empty alternative text and `aria-hidden="true"` as decorative images.
- A genuine disabled textarea.
- A genuine disabled `ADD PHOTO OR SKETCH` control.
- A genuine disabled `ASK REV` control.
- The bottom instruction at the console's lower boundary.
- An empty visible future-station region.

No generic placeholder may replace a disabled control. The disabled presentation grants no interaction, selection, entry, upload or submission authority.

## Mandatory founder walkthrough

The compact candidate must show REV, the approved contact shadow, live console, visible reserved station region and environmental floor together to determine whether REV appears physically grounded in the complete composition.

The walkthrough must not move REV or alter its shadow merely to resolve an apparent problem. It must also recheck:

1. Bottom instruction-to-console-edge clearance.
2. Disabled right-panel legibility on the real display.
3. Compact brand-to-hero clearance.

These checks authorize no automatic adjustment. Any material visual change requires another founder walkthrough.

## Explicit exclusions

- Mobile composition.
- Navigation.
- Selected or interactive states and event handlers.
- Enabled textarea, upload or `ASK REV` controls.
- Station presentation or electricity/plasma.
- Post-`ASK REV` states.
- Project creation, persistence, providers or safety processing.
- Workshop integration.
- Icon alteration or individual fine-tuning.
- Production code or tests.
- `REV-FIX-02.5`.

## Candidate stop conditions

A later candidate operation must stop without presenting a candidate if:

- any protected hash or authority link fails;
- the viewport differs from `1100 × 800`;
- the console leaves the maximum `X=40–728`, `Y=390–700` envelope;
- the intended wide, shallow console cannot remain approximately `688 × 230` without responsive visual drift;
- the `81px` console-to-REV clearance or `0px` future-station overlap cannot be preserved;
- any outer-width or REV-clearance amendment becomes necessary;
- REV placement, floor contact, receiving-hand target or contact shadow changes;
- the brand departs from `VPB-HOME-011`;
- any pathway icon is cropped, clipped, altered or individually tuned;
- three readable pathway cards cannot remain in one row at approximately `129 × 150–158`;
- the hero cannot remain visually connected beneath the protected brand on the left;
- both console panels cannot remain visible and legible without unauthorized stacking or structural reflow;
- whole-panel opacity obscures the disabled heading, supporting description or genuine controls;
- the bottom instruction cannot retain at least `8–10px` console-border clearance;
- any pathway is selected or any entry control becomes enabled;
- any control, card, border or wording becomes flattened or is replaced by a generic placeholder;
- the reserved lower station corridor is not empty and visible;
- console geometry cannot be reconstructed using separate live layers;
- browser resources fail, responses are incomplete, dimensions mismatch or rendering/console errors remain;
- any repository path or Git state changes; or
- the compact composition requires mobile work, station presentation, electricity, interaction, Project, persistence, provider, safety, Workshop or production implementation work.

## Approved Compact Iteration 2 — VPB-HOME-016

The founder-approved state is exactly `1100 × 800` at DPR `1`. Approved measurements are console `40,390 · 688 × 230`; panels `426:262` (approximately `62:38`); cards `129 × 156`; and three identical `92 × 62` icon-contain boxes. REV placement, receiving-hand target, approved contact shadow, floor contact and `81px` console clearance remain protected. The transition floor `Y=620–700` and station reserve `Y=700–800` remain empty.

All three pathway controls are unselected. The textarea, `ADD PHOTO OR SKETCH` and `ASK REV` are genuine disabled controls. Raster icons remain byte-identical, proportionally contained, decorative and accessibility-hidden. Interface wording and controls remain live semantic HTML/CSS. No flattened interface layer, Canvas, script, interaction or production implementation is approved.

The browser-verification screenshot is byte-identical to the approved clean candidate and is recorded as verification equivalence without a redundant protected copy.

Non-blocking live-integration checks remain:

1. Confirm pathway-card descriptions remain readable in the real compact runtime.
2. Confirm disabled-panel wording retains adequate contrast across displays.
3. Recheck the visual hand-to-console relationship while preserving the approved `81px` clearance.

These checks authorize no automatic adjustment. Any material change requires another founder walkthrough.

This operation authorizes documentation and visual locking only. It grants no production implementation or Build Contract. Stage 1D-C Mobile and `REV-FIX-02.5` remain locked. Staging, commit and push require separate founder authorization.
