# Stage 1D-C — Mobile Opening Console-Entry Shell Visual Contract

**Status:** FOUNDER VISUALLY APPROVED — VPB-HOME-017 DOCUMENTATION CHECKPOINT COMPLETE
**Pushed baseline:** `6540fae31af7013962400881f16ba65adb9f5f34`
**Viewport:** exactly `390 × 844`
**Active lane:** Lane 0 — continuity/control
**Active production Build Contract:** NONE

## Founder study decision

Study A — three stacked full-width pathway rows — is the founder-selected documentation basis. Study B — horizontal pathway rail — is founder rejected and cannot become authority, fallback or candidate source.

Vertical page scrolling is acceptable. All three pathway choices must remain visible together in the initial `390 × 844` viewport. The disabled entry section follows the pathway section and may continue below the initial viewport. The empty future-station reserve follows the complete blank-state content and may begin below the initial viewport.

Study A's exploratory tiny body-copy sizes are explicitly not approved and are not locked measurements. A future candidate must present improved readable typography. Readable canonical wording takes priority over reducing vertical scrolling. A horizontal pathway rail, cropped card or hidden pathway choice is prohibited.

## Founder-approved Iteration 2

Exact approval wording: `FOUNDER APPROVES STAGE 1D-C MOBILE CANDIDATE ITERATION 2`.

`VPB-HOME-017` controls the approved Stage 1D-C mobile blank-state composition. The initial viewport is `390 × 844` at DPR `1`; the full page is `390 × 1233`; required vertical scrolling is `389px`. All three stacked pathway choices are completely visible initially and remain unselected. The genuine disabled entry section follows below.

Candidate V1 is superseded because it displayed review-only reserve wording. Iteration 2 removes that wording without changing layout geometry. The empty station reserve remains `370 × 130` at `X=10–380`, `Y=1088.58–1218.58` and contains no clean label, accessible name, pseudo-element content, marker or placeholder.

REV placement, occlusion, receiving-hand target, face, badge, book and brand clearance remain protected. Raster icons remain unchanged, proportional and accessibility-hidden. Environment, REV, brand, interface, controls and icons remain separate layers. Vertical scrolling is approved. The browser-verification screenshot is byte-identical to the initial candidate and is not stored redundantly.

Non-blocking live-integration review item: recheck the visual relationship between REV's receiving hand and the trust statements on the real closed Galaxy Fold 6 without moving or rescaling REV by inference. This check authorizes no automatic adjustment.

## Protected authority relationships

- `VPB-HOME-008B` controls the mobile environment crop.
- `VPB-HOME-009B` controls mobile REV placement and foreground occlusion.
- `VPB-HOME-012` controls the live mobile brand and REV clearance.
- `VPB-HOME-014` controls the raster pathway-icon family.
- `VPB-HOME-015` controls the approved desktop blank-state composition.
- `VPB-HOME-016` controls the approved compact blank-state composition.
- `VPB-HOME-006` remains a flattened composition reference only and cannot become a runtime or candidate interface layer.

Environment, REV, brand, interface and icons remain separate layers. No protected asset may be altered, cropped, recoloured, sharpened, regenerated or silently reinterpreted.

## Mobile REV boundary

- Complete bounds: `X=95–364`, `Y=100–624`.
- Visible bounds after foreground-console occlusion: `X=95–364`, `Y=100–384`.
- Receiving-hand target: `X=113`, `Y=283`.
- Face, hand, badge and book remain visible.
- Brand-to-hair clearance remains approximately `10px`.
- Brand/REV overlap remains `0px`.
- Future-station overlap remains `0px`.
- REV must not be moved or rescaled by inference.
- The temporary dark mask remains occlusion evidence only. It is not approved console artwork, shape, colour, opacity, styling or runtime input.

## Future candidate structure

- One shared semantic DOM serves desktop, compact and mobile.
- Mobile adaptation uses CSS reflow only.
- Wording, controls, behaviour and icon sources remain identical across responsive states.
- Three full-width stacked semantic pathway buttons remain simultaneously visible and all use an unselected state.
- A genuine disabled textarea follows the pathway section.
- Genuine disabled `ADD PHOTO OR SKETCH` and `ASK REV` buttons follow with no enabled appearance or behaviour.
- Disabled heading, description and controls remain readable. Whole-panel opacity is prohibited.
- The three `VPB-HOME-014` icons remain byte-identical, proportionally contained, decorative, empty-alt and `aria-hidden="true"`; the semantic pathway control owns its accessible name and description.
- Only the environment, transparent REV and decorative icons may be image layers. Brand, hero, trust row, cards, wording, borders and controls remain live semantic HTML/CSS.
- Canvas, flattened interface content, alternate mobile application logic and mobile-only assets are prohibited.

## Evidence boundary

A separately authorized candidate package must remain outside the repository and include:

1. one initial-viewport capture at exactly `390 × 844`;
2. one full-page capture showing the complete disabled entry panel and empty future-station reserve;
3. one separate measurement guide for the initial viewport and one for the full page;
4. semantic HTML and one external CSS source;
5. the actual browser DPR used;
6. exact hero, trust, console, pathway, icon-contain, disabled-control, REV, brand, occlusion and station-reserve bounds;
7. DOM and accessibility verification;
8. protected-resource HTTP, MIME, body-length, hash and natural-dimension verification;
9. browser-console and external-request verification; and
10. repository, index and protected-hash preservation evidence.

The future candidate may establish its own readable font sizes and total page height for founder review. Neither value is locked by this documentation contract.

## Device gates

A real closed Galaxy Fold 6 walkthrough remains mandatory after live integration. The `390 × 844` browser evidence does not replace that device walkthrough. Open-Fold work and any open-posture dimensions are not authorized.

## Explicit exclusions

This contract grants `VPB-HOME-017` visual authority only. It grants no production implementation, navigation, interaction, selected state, enabled input/upload/ASK behavior, station presentation, electricity/plasma, post-`ASK REV` state, Project creation, persistence, provider work, safety processing, Workshop integration, open-Fold work or `REV-FIX-02.5` authority.

## Stop conditions

Stop without presenting a candidate if:

- any protected hash or authority link fails;
- the initial viewport differs from `390 × 844`;
- any protected environment, REV, brand or icon geometry changes;
- REV moves or rescales, or its approved bounds, visibility, hand target, occlusion or clearances fail;
- brand/REV or future-station overlap exceeds `0px`;
- the temporary mask is treated as console design;
- all three pathway choices are not visible together initially;
- a horizontal rail, cropped card or hidden pathway choice appears;
- readable canonical wording is compressed to preserve a shorter page;
- any pathway becomes selected or any disabled control becomes enabled;
- any live interface content becomes flattened;
- a separate mobile DOM, application logic, wording or asset is required;
- the disabled entry panel or empty station reserve is absent from the full-page evidence;
- any browser resource fails, response is incomplete, dimensions mismatch, an external request occurs or a console error remains;
- any repository path or Git state changes during candidate preparation; or
- completion requires production, station, electricity, post-ASK, Project, provider, Workshop, open-Fold or `REV-FIX-02.5` work.

## Next gate

Stage 1D-C Iteration 2 is visually approved and protected as `VPB-HOME-017`. Checkpoint staging, production implementation, real-device integration and any material visual adjustment require separate exact founder authorization. The real closed Galaxy Fold 6 walkthrough remains mandatory after live integration. Open-Fold work, production and `REV-FIX-02.5` remain locked; no active Build Contract exists.
